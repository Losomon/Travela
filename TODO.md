# TODO

## SCSS compilation for Admin
- [ ] Inspect existing admin CSS import source (done: admin loads precompiled `frontend/admin/assets/css/main.css`).
- [ ] Add SCSS entry file that imports `frontend/scss/bootstrap.scss`.
- [ ] Add build script to compile SCSS -> `frontend/admin/assets/css/main.css`.
- [ ] Update `frontend/package.json` with `sass` (dart-sass) devDependency and build script.
- [x] Run install + build and verify output.


